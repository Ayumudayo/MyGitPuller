<ContentDialog
    x:Class="GitPuller_WinUI.Views.Dialogs.AdvancedOptionsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Advanced sync options"
    PrimaryButtonText="Save defaults"
    CloseButtonText="Close"
    DefaultButton="Primary"
    mc:Ignorable="d">
    <ScrollViewer
        MaxHeight="640"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="10">
            <NumberBox
                x:Name="WorkersBox"
                Header="Workers"
                Minimum="1"
                Maximum="64" />
            <NumberBox
                x:Name="TimeoutBox"
                Header="Git timeout seconds"
                Minimum="1"
                Maximum="3600" />
            <NumberBox
                x:Name="StaleLockBox"
                Header="Stale lock minutes"
                Minimum="1"
                Maximum="1440" />
            <CheckBox
                x:Name="SyncAllBranchesBox"
                Content="Sync all accessible branches" />
            <CheckBox
                x:Name="StaleLockCleanupBox"
                Content="Disable stale lock cleanup" />
            <CheckBox
                x:Name="VerboseReportBox"
                Content="Verbose report" />
            <CheckBox
                x:Name="InitMissingSubmodulesBox"
                Content="Initialize missing submodules" />
            <InfoBar
                x:Name="StatusBar"
                Severity="Informational"
                Title="Options" />
            <InfoBar
                x:Name="ErrorBar"
                Severity="Error"
                Title="Save failed" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
